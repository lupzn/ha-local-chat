{
  "config": {
    "step": {
      "user": {
        "title": "Home Assistant Local Chat",
        "description": "Set up the local chat. No configuration is required — just press Submit."
      }
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single instance is possible."
    }
  }
}
